require('dotenv').config(); const { Pool } = require('pg'); const pool = new Pool({ host: process.env.DB_HOST || 'localhost', port: process.env.DB_PORT || 5432, database: process.env.DB_NAME || 'shule_sms', user: process.env.DB_USER || 'postgres', password: process.env.DB_PASSWORD, connectionTimeoutMillis: 5000 }); pool.query('SELECT NOW() as time, version() as version').then(r => { console.log('DB OK:', r.rows[0].time, r.rows[0].version); pool.end(); }).catch(e => { console.error('DB FAIL:', e.code, e.message); pool.end(); });
